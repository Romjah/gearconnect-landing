name: AI Team Together.ai
on:
  issues:
    types: [opened, edited]
  workflow_dispatch:
    inputs:
      task_description:
        description: 'Describe what you want AI Team to build'
        required: true
        default: 'Create a modern landing page'

permissions:
  contents: write
  pull-requests: write
  issues: write
  actions: read
  checks: write
  statuses: write

jobs:
  ai-team-together:
    runs-on: ubuntu-latest
    name: 🤖 AI Team Together.ai
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
          
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'
          
      - name: Setup Git
        run: |
          git config --global user.name "AI Team Together.ai"
          git config --global user.email "ai-team-together@github-actions.local"
          
      - name: 🤖 Run AI Team Together.ai
        id: ai_team
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TOGETHER_AI_API_KEY: ${{ secrets.TOGETHER_AI_API_KEY }}
          ISSUE_TITLE: ${{ github.event.issue.title || github.event.inputs.task_description }}
          ISSUE_BODY: ${{ github.event.issue.body || 'Create modern code' }}
        run: |
          # Install Python dependencies
          pip install requests python-dotenv typing-extensions
          
          chmod +x .github/scripts/ai_team_mcp.py
          python3 .github/scripts/ai_team_mcp.py
          
      - name: 🌿 Create Branch and Push
        if: steps.ai_team.outputs.changes_made == 'true'
        run: |
          BRANCH_NAME="${{ steps.ai_team.outputs.branch_name }}"
          git checkout -b $BRANCH_NAME
          git add -A
          git commit -m "🤖 ${{ steps.ai_team.outputs.agent }}: ${{ steps.ai_team.outputs.task_summary }}"
          git push origin $BRANCH_NAME
          
      - name: 🔄 Create Pull Request
        if: steps.ai_team.outputs.changes_made == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          BRANCH_NAME="${{ steps.ai_team.outputs.branch_name }}"
          AGENT="${{ steps.ai_team.outputs.agent }}"
          SUMMARY="${{ steps.ai_team.outputs.task_summary }}"
          FILES="${{ steps.ai_team.outputs.files_created }}"
          ISSUE_NUM="${{ github.event.issue.number }}"
          
          PR_TITLE="🤖 $AGENT: $SUMMARY"
          
          echo "🤖 Pull Request générée par AI Team Together.ai" > pr_body.txt
          echo "" >> pr_body.txt
          echo "Agent IA: $AGENT" >> pr_body.txt
          if [ -n "$ISSUE_NUM" ]; then
            echo "Issue: #$ISSUE_NUM" >> pr_body.txt
          else
            echo "Issue: Manual task" >> pr_body.txt
          fi
          echo "" >> pr_body.txt
          echo "Description: $SUMMARY" >> pr_body.txt
          echo "Fichiers créés: $FILES" >> pr_body.txt
          echo "" >> pr_body.txt
          echo "Technologie: Together.ai + AI Team Orchestrator" >> pr_body.txt
          echo "" >> pr_body.txt
          echo "Créée automatiquement par AI Team Orchestrator + Together.ai" >> pr_body.txt
          
          if [ -n "$ISSUE_NUM" ]; then
            echo "" >> pr_body.txt
            echo "Closes #$ISSUE_NUM" >> pr_body.txt
          fi
          
          gh pr create --title "$PR_TITLE" --body-file pr_body.txt --head "$BRANCH_NAME" --base main
          rm pr_body.txt
          
      - name: 💬 Comment Success
        if: steps.ai_team.outputs.changes_made == 'true' && github.event.issue.number
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const agent = '${{ steps.ai_team.outputs.agent }}';
            const branchName = '${{ steps.ai_team.outputs.branch_name }}';
            
            let commentBody = '🎉 Pull Request créée avec Together.ai !\n\n';
            commentBody += 'Agent utilisé: ' + agent + '\n';
            commentBody += 'Branche: ' + branchName + '\n\n';
            commentBody += 'Prochaines étapes:\n';
            commentBody += '1. Review le code généré dans la PR\n';
            commentBody += '2. Tester les fonctionnalités\n';
            commentBody += '3. Merger la PR si approuvé\n\n';
            commentBody += 'AI Team Orchestrator v1.3.0 + Together.ai';

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: commentBody
            });
            
      - name: 💬 Comment No Changes
        if: steps.ai_team.outputs.changes_made != 'true' && github.event.issue.number
        uses: actions/github-script@v7
        with:
          script: |
            let commentBody = '## 🤖 AI Team MCP a analysé votre demande\n\n';
            commentBody += '**Agent assigné:** ${{ steps.ai_team.outputs.agent }}\n';
            commentBody += '**Tâche:** ${{ steps.ai_team.outputs.task_summary }}\n\n';
            commentBody += '⚠️ **Aucun fichier généré**\n\n';
            commentBody += 'L\'agent a analysé mais n\'a pas créé de fichiers.\n\n';
            commentBody += '💡 **Suggestions:**\n';
            commentBody += '- Reformulez de manière plus spécifique\n';
            commentBody += '- Précisez le type de code souhaité\n';
            commentBody += '- Ajoutez des détails techniques\n\n';
            commentBody += '---\n';
            commentBody += '*AI Team Orchestrator v1.3.0 avec Together.ai*';

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: commentBody
            });
            
      - name: 🎉 Summary
        run: |
          echo "🤖 AI Team Together.ai completed!"
          echo "Agent: ${{ steps.ai_team.outputs.agent }}"
          echo "Task: ${{ steps.ai_team.outputs.task_summary }}"
          echo "Changes: ${{ steps.ai_team.outputs.changes_made }}" 